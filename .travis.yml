language: cpp

sudo: false

compiler:
    - g++
os:
    - linux

before_script:
    - wget -q -O - https://boostorg.jfrog.io/artifactory/main/release/1.79.0/source/boost_1_79_0.tar.gz | tar -xz

script:
    - make
